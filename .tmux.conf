#

unbind-key C-b					# Reset prefix-key
set-option -g prefix C-t			# Set prefix-key C-t

set-option -g mode-keys vi			# Use vi key bindings in copy mode.
set-option -g mouse on

set-option -g base-index 1 			# Set the base index from which an unused index should be searched when a new window is created.

set-window-option -g monitor-activity on	# set monitor activity

set-option -g status-left-length 20
set-option -g status-left '[#[underscore]#S#[default]] ' 

set-option -g status-right-length 60	# Set the maximum length of the right/left component of the status line.
set-option -g status-right '{#H} [%Y-%m-%d(%a) %H:%M]' # Wi-Fi、バッテリー残量、現在時刻

set-window-option -g window-status-format " #I:#W{-} " 
set-window-option -g window-status-current-format "#[reverse,bold]|#I:#W{#P}|" 

set-option -g status-interval 1		# Update the status line every 1 seconds.
set-option -g status-fg "colour255"	# Set the foreground colour in the status.
set-option -g status-bg "colour70"	# Set the background colour in the status.

bind-key X kill-window				# Kill window
bind-key d detach				# Detach
bind-key i display-panes			# Display pane number
bind-key b break-pane				# Break pane
bind-key r source-file ~/.tmux.conf		# Source configure file
bind-key e setw synchronize-panes \; display "synchronize-panes #{?pane_synchronized,on,off}"
bind-key ! split-window -h

bind-key h select-pane -L			# Moving panes
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

bind-key -r H resize-pane -L 5
bind-key -r J resize-pane -D 5
bind-key -r K resize-pane -U 5
bind-key -r L resize-pane -R 5

bind-key -T copy-mode-vi v send -X begin-selection	# Start selection per charactor with 'v'.
bind-key -T copy-mode-vi V send -X select-line		# Start selection linewise with 'V'
bind-key -T copy-mode-vi C-v send -X rectangle-toggle	# Start selection blockwise with 'C-v'
#bind-key -T copy-mode-vi y send -X copy-selection	# Yank with 'y'
bind-key -T copy-mode-vi Y send -X copy-line		# Yank lines with 'Y'
bind-key C-p paste-buffer				# Put the text after the cursor with 'C-p'
## Cooperation with the clipboard
### MacOS
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"
bind-key -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "pbcopy"
