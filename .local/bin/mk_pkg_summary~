#/bin/sh

mk_pkg_summary() {
	\cd /usr/pkgsrc/packages/All && /usr/pkg/sbin/pkg_info -X *.tgz | gzip > ./pkg_summary.gz
}
if [ -d /usr/pkgsrc ] ; then
	mk_pkg_summary
else
	echo "this computer does not have pkgsrc."
fi

